<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/editContact.css">
    <title><%= titlePage %></title>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Edit Kontak</h1>
            <p><%= contact.name %></p>
        </div>
        <div class="back-btn">
            <svg xmlns="http://www.w3.org/2000/svg" height="32px" viewBox="0 -960 960 960" width="32px" fill="#2e2e2e"><path d="m313-440 224 224-57 56-320-320 320-320 57 56-224 224h487v80H313Z"/></svg>
        </div>
        <div class="img">
            
        </div>
        <form action="/edit/<%= contact._id %>?_method=PUT" method="POST">
            <div class="input-name">
                <input type="text" name="name" id="name" placeholder="<%= contact.name %>">
            </div>
            <div class="input-email">
                <input type="email" name="email" id="email" placeholder="<%= contact.email %>" value="<%= contact.email %>">
            </div>
            <div class="input-number">
                <input type="text" name="number" id="number" placeholder="<%= contact.number %>" value="<%= contact.number %>">
            </div>
            <button class="save-btn" type="submit">
                <span>simpan</span>
            </button>
        </form>
    </div>

    <script>
        document.querySelector('.back-btn').addEventListener('click', function () {
            window.location.href = "/contact/<%= contact._id %>";
        });
    </script>
</body>
</html>